<% if flash[:errors] %>
  <% flash[:errors].each do |error| %>
    <%= p error %>
  <% end %>
<% end %>

<%= form_for @heroine do |f| %>
<label>Heroine Name:</label>
  <%= f.text_field :name %>
  <br>
  <br>
  <label>heroine super name:</label>
  <%= f.text_field :super_name %>
  <br>
    <%= f.collection_select :power_id, Power.all, :id, :name %>
  <%= f.submit %>

<% end %>
